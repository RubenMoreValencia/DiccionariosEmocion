% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualizacion_modelos.R
\name{graficar_pca_emociones_etiquetas}
\alias{graficar_pca_emociones_etiquetas}
\title{Graficar PCA de emociones con etiquetas y biplot combinado}
\usage{
graficar_pca_emociones_etiquetas(
  pca,
  data,
  labels_cluster,
  labels_texto = NULL,
  componentes = c(1, 2),
  titulo = "PCA de emociones",
  show_labels = TRUE,
  repel = TRUE,
  ellipse = TRUE,
  ellipse_type = "norm",
  ellipse_level = 0.95,
  palette = NULL,
  point_size = 1.8,
  point_alpha = 0.75,
  loading_scale = 1,
  top_n_loadings = NULL,
  biplot = FALSE,
  biplot_alpha = 0.9
)
}
\arguments{
\item{pca}{Objeto de clase \code{prcomp}.}

\item{data}{\code{data.frame} con las variables usadas para el PCA.}

\item{labels_cluster}{Vector con la etiqueta de clúster/grupo (longitud = nrow(data)).}

\item{labels_texto}{(opcional) Vector de etiquetas para cada punto. Si \code{NULL} o longitud distinta,
se asignan \code{ID_1}, \code{ID_2}, etc.}

\item{componentes}{Integer vector con PCs a proyectar. Por defecto \code{c(1, 2)}.}

\item{titulo}{Título base. Por defecto \code{"PCA de emociones"}.}

\item{show_labels}{Logical, mostrar etiquetas de puntos. Por defecto \code{TRUE}.}

\item{repel}{Logical, usar \pkg{ggrepel} para evitar solapamientos. Por defecto \code{TRUE}.}

\item{ellipse}{Logical, dibujar elipses por grupo. Por defecto \code{TRUE}.}

\item{ellipse_type}{\code{"norm"}, \code{"t"} o \code{"euclid"}. Por defecto \code{"norm"}.}

\item{ellipse_level}{Nivel de confianza (p. ej. 0.95). Por defecto \code{0.95}.}

\item{palette}{\code{NULL}, nombre de paleta de \pkg{ggplot2} (p. ej. \code{"Dark2"}) o vector de colores.}

\item{point_size}{Tamaño de punto. Por defecto \code{1.8}.}

\item{point_alpha}{Alpha de puntos. Por defecto \code{0.75}.}

\item{loading_scale}{Factor extra para escalar flechas de cargas (2D). Por defecto \code{1}.}

\item{top_n_loadings}{\code{NULL} o entero: etiqueta solo las \code{top_n_loadings} variables por norma.}

\item{biplot}{Logical. Si \code{TRUE} y \code{length(componentes)==2}, devuelve \code{biplot} combinado. Por defecto \code{FALSE}.}

\item{biplot_alpha}{Escala relativa de auto–ajuste de cargas al rango de scores en el panel \code{[0,1]}. Por defecto \code{0.9}.}

\item{2}{(auto) TODO: describir parámetro.}
}
\value{
Lista con:
\itemize{
\item \code{scatter}, \code{elipses}, \code{cargas} (si 2D),
\item \code{biplot} (si \code{biplot=TRUE} y 2D),
\item \code{data_proyectada}, \code{loadings}, \code{var_exp}.
}
}
\description{
Genera visualizaciones a partir de un objeto \code{prcomp()}:
(1) dispersión coloreada por clúster con etiquetas opcionales,
(2) dispersión con elipses por grupo,
(3) vectores de carga (solo si 2D),
(4) biplot combinado (puntos + cargas en el mismo panel, con auto-escalado).
Los ejes incluyen el \\% de varianza explicada.
}
\examples{
\dontrun{
pca <- prcomp(acdfx_emoc, center = TRUE, scale. = TRUE)
res <- graficar_pca_emociones_etiquetas(
  pca, acdfx_emoc, labels_cluster = acdfx$categoria,
  labels_texto = rownames(acdfx_emoc),
  palette = "Dark2", top_n_loadings = 10,
  biplot = TRUE, biplot_alpha = 0.9
)
print(res$scatter); print(res$elipses); print(res$cargas); print(res$biplot)
}
}
\seealso{
Other reduccion_dimensional: 
\code{\link{ajustar_pca_svm}()},
\code{\link{estrategia_p4_pca_cluster}()},
\code{\link{generar_pca_emocional}()},
\code{\link{graficar_pca_emociones}()},
\code{\link{pca_tokens_biplot}()},
\code{\link{pca_tokens_plot}()},
\code{\link{resModeloPCA}()},
\code{\link{run_pca}()},
\code{\link{simular_pca_svm}()}
}
\concept{reduccion_dimensional}
