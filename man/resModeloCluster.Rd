% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ca_pca_cluster.R
\name{resModeloCluster}
\alias{resModeloCluster}
\title{resModeloCluster}
\usage{
resModeloCluster(
  dataClus,
  indiceClase,
  vectorClase,
  scale_unit = TRUE,
  ncp = NULL,
  hcpc_args = list(nb.clust = -1)
)
}
\arguments{
\item{dataClus}{\code{data.frame} con variables numéricas y una columna de clase.}

\item{indiceClase}{\code{integer}. Índice (1..p) de la columna de clase a excluir del PCA.}

\item{vectorClase}{\code{factor} o \code{character}. Etiquetas para colorear individuos en los plots.
Debe tener longitud igual al número de filas de \code{dataClus}.}

\item{scale_unit}{\code{logical}. Si \code{TRUE}, estandariza variables a varianza 1
en la PCA (equivalente a \code{scale.unit=TRUE} en \code{FactoMineR::PCA}).}

\item{ncp}{\code{integer} o \code{NULL}. Número de componentes a retener en la PCA.
Si \code{NULL}, usa \code{min(5, ncol(X))}.}

\item{hcpc_args}{\code{list}. Parámetros extra para \code{FactoMineR::HCPC}
(por ejemplo, \code{list(nb.clust = -1)} para detección automática de k).}
}
\value{
\code{list} con:
\itemize{
\item \code{modelo} objeto \code{PCA}.
\item \code{propVarianza} data.frame de eigenvalores (\code{factoextra::get_eigenvalue}).
\item \code{variablesActivas} lista de cargas/coord/cos2 (\code{factoextra::get_pca_var}).
\item \code{hcpc} objeto \code{HCPC}.
\item \code{filas_usadas} \code{integer} índices de filas conservadas (tras \code{na.omit}).
\item \code{graficaCluster} ggplot de clusters.
\item \code{graficaDend} ggplot del dendrograma.
\item \code{graficaInd} ggplot de individuos (PCA) coloreados por \code{vectorClase}.
\item \code{graficaDim} ggplot (individuos + elipses por grupo).
\item \code{graficaCof} ggplot (elipses de confianza por grupo).
\item \code{graficaBiplot}, \code{graficaBiplot2} biplots listos.
}
}
\description{
Ejecuta un flujo de \strong{PCA} (FactoMineR) sobre variables numéricas
(excluyendo una columna de clase), seguido de \strong{HCPC} (clustering jerárquico
sobre componentes). Incluye escalamiento opcional (varianza unitaria) y retorna
objetos de graficación listos via \code{factoextra}.
}
\details{
PCA + HCPC con (opcional) escalamiento y graficas listas

La función:
\enumerate{
\item Verifica tipos y longitudes; 2) excluye la columna de clase del PCA;
\item elimina filas con NA de manera segura (\code{na.omit}); 4) remueve columnas
con varianza cero; 5) corre \code{PCA} con centrado y escalamiento opcional;
\item aplica \code{HCPC} al resultado de la PCA; 7) construye visualizaciones
(cluster plot, dendrograma, mapas PCA por grupos).
}
}
\examples{
\dontrun{
library(FactoMineR); library(factoextra)
df <- iris
df$Species <- factor(df$Species)
# Excluir clase (columna 5) del PCA; colorear por Species
res <- resModeloCluster(
  dataClus    = df,
  indiceClase = 5,
  vectorClase = df$Species,
  scale_unit  = TRUE,
  hcpc_args   = list(nb.clust = -1)  # elige k automáticamente
)
res$graficaCluster
res$graficaDend
res$graficaBiplot
}
}
\seealso{
\code{\link[FactoMineR]{PCA}}, \code{\link[FactoMineR]{HCPC}}

Other modelado_supervisado: 
\code{\link{ajustar_modelo_polinomial}()},
\code{\link{ajustar_svm_multiclase}()},
\code{\link{entrenar_modelo_nivel}()},
\code{\link{entrenar_modelos_emocionales}()},
\code{\link{resModeloCA}()},
\code{\link{resModeloMFA}()},
\code{\link{simular_svm_multiclase}()}
}
\concept{modelado_supervisado}
