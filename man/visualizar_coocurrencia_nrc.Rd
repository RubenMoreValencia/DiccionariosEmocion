% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aplicacion_texto.R, R/visualizacion_modelos.R
\name{visualizar_coocurrencia_nrc}
\alias{visualizar_coocurrencia_nrc}
\title{Visualizar matriz de coocurrencia NRC}
\usage{
visualizar_coocurrencia_nrc(
  matriz,
  tipo = c("heatmap", "red"),
  normalizar = c("none", "prop", "max"),
  diag_cero = TRUE,
  ordenar = c("hclust", "sum", "none"),
  palette = NULL,
  min_edge = 0,
  top_edges = NULL,
  layout_red = "fr"
)

visualizar_coocurrencia_nrc(
  matriz,
  tipo = c("heatmap", "red"),
  normalizar = c("none", "prop", "max"),
  diag_cero = TRUE,
  ordenar = c("hclust", "sum", "none"),
  palette = NULL,
  min_edge = 0,
  top_edges = NULL,
  layout_red = "fr"
)
}
\arguments{
\item{matriz}{\code{matrix}/\code{data.frame} cuadrada con nombres de fila/columna (emociones).}

\item{tipo}{\code{c("heatmap","red")}. Tipo de visualización.}

\item{normalizar}{\code{c("none","prop","max")}. \code{"prop"} divide por la suma total;
\code{"max"} divide por el valor máximo; \code{"none"} no normaliza. Por defecto \code{"none"}.}

\item{diag_cero}{\code{logical}. Si \code{TRUE}, pone la diagonal en 0 (evita auto-coocurrencia).
Por defecto \code{TRUE}.}

\item{ordenar}{\code{c("none","sum","hclust")}. Reordena emociones por suma decreciente o clustering
jerárquico (\code{hclust}) sobre la matriz simétrica. Por defecto \code{"hclust"}.}

\item{palette}{\code{character} o vector de colores. Si \code{NULL}, usa \code{viridis}.}

\item{min_edge}{\code{numeric}. Umbral mínimo de peso de arista para la red (excluye menores).
Por defecto \code{0}.}

\item{top_edges}{\code{integer} opcional. Si se indica, mantiene solo las \code{top_edges} aristas
más pesadas (tras aplicar \code{min_edge}).}

\item{layout_red}{\code{character}. Layout de \pkg{ggraph} para la red. Por defecto \code{"fr"}.}

\item{"red"}{(auto) TODO: describir parámetro.}
}
\value{
Gráfico mostrado en pantalla.

Una \code{list} con:
\itemize{
\item \code{matriz_procesada}: matriz tras normalización/orden/diagonal.
\item \code{datos_long}: \code{data.frame} largo (para heatmap).
\item \code{edges}: \code{data.frame} de aristas (para red).
\item \code{grafico}: objeto \code{ggplot}.
}
}
\description{
Muestra visualmente la matriz de co-ocurrencia de emociones NRC como heatmap o red.

Muestra la co-ocurrencia de emociones NRC como \emph{heatmap} o \emph{red}.
Permite normalización, ordenamiento y filtrado por peso mínimo/top-M aristas.
}
\examples{
visualizar_coocurrencia_nrc(cooc, tipo = "heatmap")
\dontrun{
# Suponiendo que ya construiste 'cooc' (matriz de co-ocurrencia NRC):
v1 <- visualizar_coocurrencia_nrc(cooc, tipo = "heatmap", normalizar = "prop")
print(v1$grafico)

v2 <- visualizar_coocurrencia_nrc(cooc, tipo = "red", normalizar = "max",
                                  min_edge = 0.05, top_edges = 50, layout_red = "fr")
print(v2$grafico)
}
}
\seealso{
Other matrices_valoradas: 
\code{\link{MatrizValoradaXMetodo}()},
\code{\link{visualizar_coocurrencia_nrc_v2}()},
\code{\link{visualizar_emociones_nrc}()}
}
\concept{matrices_valoradas}
